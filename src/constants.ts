export enum Inputs {
  Owner = 'owner',
  Repo = 'repo',
  RunId = 'runId',
  Token = 'token',
  Latest = 'latest',
  Name = 'name'
}

export enum Outputs {
  Failed = 'failed',
  Deleted = 'deleted',
  Artifacts = 'artifacts'
}
